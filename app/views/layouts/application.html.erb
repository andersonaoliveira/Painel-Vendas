<!DOCTYPE html>
<html class="ls-theme-green">
  <head>
    <title>Vendas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
    <link href="http://assets.locaweb.com.br/locastyle/3.10.1/stylesheets/locastyle.css" rel="stylesheet" type="text/css">
    <link rel="icon" sizes="192x192" href="/locawebstyle/assets/images/ico-boilerplate.png">
    <link rel="apple-touch-icon" href="/locawebstyle/assets/images/ico-boilerplate.png">
    <link rel="stylesheet" type="text/css" href="//assets.locaweb.com.br/locastyle/3.10.1/stylesheets/locastyle.css">
  </head>

  <body>
    <%= render partial: "layouts/nav" %>
    <main class="ls-main"> 
      <div class="container-fluid">
        <p class="notice ls-alert-success"><%= notice %></p>
        <p class="alert ls-alert-danger"><%= alert %></p>      
      
        <%= yield %>   
      </div>  
    </main>

    <aside class="ls-sidebar">
      <div class="ls-sidebar-inner">
        <nav class="ls-menu">
          <ul>

            <li class=<%= current_page?(root_path) ? "ls-active" : "" %>>
              <%= link_to 'Início', root_path, class:"ls-ico-home" %>
            </li>
            <% if user_signed_in? %>

            <li class=<%= current_page?(clients_path) ? "ls-active" : "" %>>
              <%= link_to 'Clientes', clients_path, class:"ls-ico-users" %>
            </li>

            <li class=<%= current_page?(orders_path) ? "ls-active" : "" %>>
              <%= link_to 'Pedidos', orders_path, class: "ls-ico-edit-admin" %>
            </li> 

            <% if current_user.admin? %>

              <li class=<%= current_page?(sellers_path) ? "ls-active" : "" %>>
                <%= link_to 'Vendedores', sellers_path , class:"ls-ico-panel-pabx"%>
              </li>

              <li class=<%= current_page?(new_seller_path) ? "ls-active" : "" %>>
                <%= link_to 'Cadastrar vendedor', new_seller_path, class:"ls-ico-user-add" %>
              </li>              

            <% end %> 
                           
              <li>
                <% commission_text = current_user.admin? ? 'Comissões' : 'Minhas Comissões' %>
                <%= link_to commission_text, commissions_path, class:"ls-ico-trophy" %>
              </li>
            
          <% end %>
        </ul>
        </nav>
      </div>
    </aside>
    <script src="http://code.jquery.com/jquery-2.0.1.min.js"></script>
    <script src="http://assets.locaweb.com.br/locastyle/3.10.1/javascripts/locastyle.js"></script>
  </body>
</html>
